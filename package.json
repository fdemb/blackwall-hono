{
  "name": "blackwall",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --filter=* dev",
    "build:cli": "bun run --filter blackwall build"
  },
  "patchedDependencies": {
    "tinykeys@3.0.0": "patches/tinykeys@3.0.0.patch"
  },
  "dependencies": {
    "oxfmt": "^0.27.0",
    "oxlint": "^1.42.0"
  }
}
